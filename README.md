# bridge-grok

Grok plugin adapter for Agiterra Bridge. Client-side plugin (skills + MCP). **No inbound Wire SSE** — that is `grok-wire` (sidecar), analog of `codex-wire`.

Same `bridge-tools` as `bridge-claude-code`. Claude Code keeps `bridge-claude-code`.

```
grok plugin install /Users/tim/Projects/Agiterra/bridge-grok --trust
```
